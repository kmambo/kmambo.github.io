apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kmambo-operator-chart.fullname" . }}-test-connection"
  labels:
    {{- include "kmambo-operator-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kmambo-operator-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
